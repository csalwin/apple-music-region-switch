services:
  dev:
    build: .
    volumes:
      - .:/app
      - amtransfer_node_modules:/app/node_modules
    env_file:
      - .env
    tty: true
    stdin_open: true
    working_dir: /app

volumes:
  amtransfer_node_modules:
